plugins {
    id 'java'
    id 'idea'
    id 'eclipse'
    id 'maven-publish'
    id 'net.minecraftforge.gradle' version '[7.0.17,8)'
}

version = mod_version
group = mod_group_id

base {
    archivesName = mod_id
}

// Forge 26.1 ships Java 25 to end users
java.toolchain.languageVersion = JavaLanguageVersion.of(25)

println "Java: ${System.getProperty 'java.version'}, JVM: ${System.getProperty 'java.vm.version'} (${System.getProperty 'java.vendor'}), Arch: ${System.getProperty 'os.arch'}"

// Include resources generated by data generators (client + server outputs live in
// sibling subdirs so each task's HashCache doesn't wipe the other on the next run).
sourceSets.main.resources {
    srcDir 'src/generated/resources/client'
    srcDir 'src/generated/resources/server'
}

// Sources excluded from compile:
//   - compat/jei/**          - JEI 26.1.2 only publishes fabric/neoforge variants.
//   - compat/wthit/**        - WTHIT latest forge-19.0.1 predates Forge 26.1.x.
//   - compat/jer/**          - JER has no 26.1 build on Modrinth.
//   - client/UsefulToolsConfigScreen.java
//   - client/ClientConfigRegistration.java
//                            - Cloth Config last forge build was 17.0.144 (MC 1.21).
//   - datagen/**             - Forge 26.1 ModelProvider / BlockTagsProvider constructor
//                              signatures diverged from the NeoForge version the port
//                              started from. The generated resources under
//                              src/generated/resources/ (~2900 files) cover everything
//                              datagen produced for the NeoForge build, so the runtime
//                              has the data it needs. Re-port the providers when datagen
//                              is needed again (recipe / blockstate edits).
sourceSets.main.java {
    exclude 'com/stonytark/usefultoolsmod/compat/jei/**'
    exclude 'com/stonytark/usefultoolsmod/compat/wthit/**'
    exclude 'com/stonytark/usefultoolsmod/compat/jer/**'
    exclude 'com/stonytark/usefultoolsmod/client/UsefulToolsConfigScreen.java'
    exclude 'com/stonytark/usefultoolsmod/client/ClientConfigRegistration.java'
    exclude 'com/stonytark/usefultoolsmod/datagen/**'
}

minecraft {
    runs {
        configureEach {
            workingDir = layout.projectDirectory.dir('run')

            // Strict runtime checks reject any method on a scanned mod class whose first
            // parameter extends Event but isn't @SubscribeEvent-annotated — even if the
            // method is registered programmatically via EventBus#addListener. The mod's
            // main class hosts commonSetup() and addCreative() that way, so leave the
            // checker off in dev.
            systemProperty 'eventbus.api.strictRuntimeChecks', 'false'
            systemProperty 'forge.enabledGameTestNamespaces', mod_id
            systemProperty 'forge.logging.markers', 'REGISTRIES'
            systemProperty 'forge.logging.console.level', 'debug'
        }

        register('client')

        register('server') {
            args '--nogui'
        }

        register('gameTestServer')

        // Two datagen runs targeting their own subdirs so the per-task HashCache doesn't
        // wipe the other's outputs as "stale" on the next run. Both subdirs are wired
        // into the main resource set above.
        register('clientData') {
            workingDir = layout.projectDirectory.dir('run-data')
            args '--mod', mod_id, '--all',
                 '--output', layout.projectDirectory.dir('src/generated/resources/client'),
                 '--existing', layout.projectDirectory.dir('src/main/resources')
        }

        register('serverData') {
            workingDir = layout.projectDirectory.dir('run-data')
            args '--mod', mod_id, '--all',
                 '--output', layout.projectDirectory.dir('src/generated/resources/server'),
                 '--existing', layout.projectDirectory.dir('src/main/resources')
        }
    }
}

repositories {
    minecraft.mavenizer(it)
    maven fg.forgeMaven
    maven fg.minecraftLibsMaven
    mavenCentral()
}

dependencies {
    implementation minecraft.dependency("net.minecraftforge:forge:${forge_version}")
    annotationProcessor 'net.minecraftforge:eventbus-validator:7.0.1'

    // Integration deps (JEI / WTHIT / Cloth Config / JER) are intentionally omitted -
    // see gradle.properties for the per-dep rationale. The compat/ sources stay
    // excluded from compileJava in sourceSets above until the upstream projects publish
    // Forge 26.1.x builds.
}

// Expand mod metadata properties into mods.toml and pack.mcmeta at build time.
tasks.named('processResources', ProcessResources).configure {
    var replaceProperties = [
            minecraft_version      : minecraft_version,
            minecraft_version_range: minecraft_version_range,
            forge_version          : forge_version,
            forge_version_range    : forge_version_range,
            loader_version_range   : loader_version_range,
            mod_id                 : mod_id,
            mod_name               : mod_name,
            mod_license            : mod_license,
            mod_version            : mod_version,
            mod_authors            : mod_authors,
            mod_description        : mod_description
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/mods.toml', 'pack.mcmeta']) {
        expand replaceProperties
    }
}

tasks.named('jar', Jar).configure {
    manifest {
        attributes([
            'Specification-Title'    : mod_id,
            'Specification-Vendor'   : mod_authors,
            'Specification-Version'  : '1',
            'Implementation-Title'   : project.name,
            'Implementation-Version' : project.jar.archiveVersion,
            'Implementation-Vendor'  : mod_authors
        ])
    }
}

publishing {
    publications {
        register('mavenJava', MavenPublication) {
            artifact jar
        }
    }
    repositories {
        maven {
            url "file://${project.projectDir}/mcmodsrepo"
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

idea.module { downloadJavadoc = downloadSources = true }
